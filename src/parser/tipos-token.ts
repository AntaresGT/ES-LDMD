/**
 * @archivo tipos-token.ts
 * @descripcion Re-exportación del enum TipoToken desde el dominio.
 * Proporciona acceso directo al enum para uso en el parser.
 */

export { TipoToken } from '@/dominio/tipos';
